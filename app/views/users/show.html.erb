<div class="container">
	<div class = "raw">
		<div class="col-md-08">
			<h1><%= "Welcome #{@user.username}!" %></h1>
			<p>Find me in app/views/users/show.html.erb</p>

			<p>The following functions need to live on this page:</p>

			<p>Some basic profile information.</p>

			<p>Name</p>
			<h2><%= "#{@user.fname} #{@user.lname}" %></h2>
			<p>Address</p>
			<h2><%= @user.address %></h2>
			<p>Phone</p>
			<h2><%= @user.phone %></h2>
			<p>Email</p>
			<h2><%= @user.email %></h2>
			<p>Likes</p>
			<h2><%= @user.likes %></h2>
			<p>Dislikes</p>
			<h2><%= @user.dislikes %></h2>
			<p>Additional Info</p>
			<h2><%= @user.add_info %></h2>

			<% if current_user == @user %>
				<p>Link to edit profile</p>
				<%= link_to "Edit Profile", edit_user_registration_path %>

				<p>link to create an event - or duplicate an event with users.</p>
				<%= link_to "Add an Event", new_event_path %>
			<% else %>
			<% end %>

			<p>Should show clickable impending events in one section</p>

			<% @pending_events.each do |event| %>
			<p>
				<%= link_to event.name, event_path(event.id) %>
			</p>
			<% end %>

			<p>should show clickable ended events in another</p>
			<% @past_events.each do |event| %>
			<p>
				<%= link_to event.name, event_path(event.id) %>
			</p>
			<% end %>
		</div>
	</div>
</div>
